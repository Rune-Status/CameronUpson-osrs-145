package com.jagex;

public class BoundingBoxDrawType { //most likely originally an enum, but the obfuscator obviously removes that

    public static final BoundingBoxDrawType ALL = new BoundingBoxDrawType();
    public static final BoundingBoxDrawType MOUSE_OVER = new BoundingBoxDrawType();

}
